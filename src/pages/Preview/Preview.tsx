import PreviewCard from '../../components/PreviewCard';
import { StyledPreview } from './style';

const Preview = () => {
  return (
    <StyledPreview>
      <PreviewCard />
    </StyledPreview>
  );
};

export default Preview;
